<h1>試合カード一覧</h1>

<% if @matches.present? %>
  <div class="match-cards">
    <% @matches.each do |match| %>
      <div class="match-card">
        <p><%= match.date %> - 何節: <%= match.round %> - リーグ: <%= match.league %> - <%= match.home_team_name %> vs <%= match.away_team_name %></p>
        
        <% if current_user == match.user %>
        <%= link_to '詳細', match_path(match) %>
        <% end %>
      </div>
    <% end %>
  </div>
<% else %>
  <p>試合カードがまだ作成されていません。</p>
<% end %>
